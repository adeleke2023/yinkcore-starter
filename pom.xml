<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.yinkcore</groupId>
    <artifactId>yinkcore-starter</artifactId>
    <version>1.0.0-SNAPSHOT</version>

    <packaging>pom</packaging>

    <name>YinkCore Starter</name>
    <description>Enterprise Modular Spring Boot Foundation</description>

    <properties>

        <java.version>25</java.version>

        <spring.boot.version>3.5.14</spring.boot.version>

        <jjwt.version>0.12.6</jjwt.version>

        <mapstruct.version>1.6.0</mapstruct.version>

    </properties>

    <dependencyManagement>

        <dependencies>

            <!-- Spring Boot Dependencies -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Testcontainers BOM  -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>1.21.3</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            
            <dependency>
                <groupId>com.github.tomakehurst</groupId>
                <artifactId>wiremock-jre8</artifactId>
                <version>3.0.1</version>
            </dependency>

            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>0.12.7</version>
            </dependency>

            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>0.12.7</version>
            </dependency>

            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>0.12.7</version>
            </dependency>


        </dependencies>

    </dependencyManagement>

    <!-- ========================= -->
    <!-- MODULES -->
    <!-- ========================= -->

    <modules>

        <!-- Apps -->
        <module>apps/api-server</module>

        <!-- Business Modules -->
        <module>modules/shared-kernel</module>
        <module>modules/auth</module>
        <module>modules/user</module>
        <module>modules/notification</module>
        <module>modules/audit</module>
        <module>modules/testing</module>
        <module>modules/outbox</module>

        <!-- Infrastructure modules -->
        <module>infrastructure/security</module>
        <module>infrastructure/persistence</module>
        <module>infrastructure/logging</module>
        <module>infrastructure/cache</module>
        <module>infrastructure/resilience</module>
        <module>infrastructure/ratelimit</module>
        <!-- <module>infrastructure/notification</module> -->

    </modules>

    <build>

        <pluginManagement>

            <plugins>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>

                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                    </configuration>
                </plugin>

            </plugins>

        </pluginManagement>

    </build>

</project>